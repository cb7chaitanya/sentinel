# Root-level convenience file listing every variable used across services.
# Each service also has its own apps/<name>/.env.example / services/<name>/.env.example
# for independent local development.

POSTGRES_USER=sentinel
POSTGRES_PASSWORD=sentinel
POSTGRES_DB=sentinel

ANTHROPIC_API_KEY=
NEXT_PUBLIC_GATEWAY_URL=http://localhost:8000
