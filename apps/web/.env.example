NEXT_PUBLIC_GATEWAY_URL=http://localhost:8000

# No warehouse registry/discovery endpoint exists yet -- the dashboard is
# pointed at one warehouse via this id until one does.
NEXT_PUBLIC_WAREHOUSE_ID=
